<h1>Wishes#index</h1>
<p>Find me in app/views/wishes/index.html.erb</p>

  <ul>
    <% @wishes.each do |wish| %>
      <li>
        <%= link_to wish.product.name, product_path(wish.product.id) %>
      </li>
    <% end %>
  </ul>